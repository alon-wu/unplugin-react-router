export default function Dashboard() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>
        A route group used purely for organization:{' '}
        <code>(admin)/dashboard.tsx</code> → <code>/dashboard</code>, no layout
        involved.
      </p>
    </main>
  )
}
